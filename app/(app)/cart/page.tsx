import { CheckoutForm } from "@/components/shared/CheckoutForm";

export const metadata = {
  title: "Checkout — Kantin 40",
  description: "Selesaikan pesananmu di Kantin 40.",
};

export default function CartPage() {
  return <CheckoutForm />;
}
