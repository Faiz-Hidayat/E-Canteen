import { CheckoutForm } from "@/components/shared/CheckoutForm";

export const metadata = {
  title: "Checkout — E-Canteen",
  description: "Selesaikan pesananmu di E-Canteen.",
};

export default function CartPage() {
  return <CheckoutForm />;
}
